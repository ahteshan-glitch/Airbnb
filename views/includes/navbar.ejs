<style>
/* Custom Search Bar Styling */
.search-bar-container {
    position: relative;
    max-width: 600px;
    margin: 0 auto;
}

.search-input {
    border-radius: 30px;
    /* Increase right padding to 120px to leave ample space for the button */
    padding: 12px 120px 12px 50px !important;
    border: 2px solid #e0e0e0;
    font-size: 1rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    background: #ffffff;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
}

.search-input:focus {
    border-color: #6366f1;
    box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
    outline: none;
}

.search-button {
    position: absolute;
    right: 10px;  /* Adjusted from 5px for better spacing */
    top: 50%;
    transform: translateY(-50%);
    border-radius: 25px;
    padding: 8px 20px;
    background: linear-gradient(135deg, #6366f1, #4f46e5);
    border: none;
    color: white;
    font-weight: 500;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 8px;
}

.search-button:hover {
    transform: translateY(-50%) scale(1.05);
    box-shadow: 0 4px 12px rgba(79, 70, 229, 0.3);
}

.search-icon {
    position: absolute;
    left: 15px;
    top: 50%;
    transform: translateY(-50%);
    color: #94a3b8;
    pointer-events: none;
}

/* Animated placeholder */
.search-input::placeholder {
    color: #94a3b8;
    transition: all 0.3s ease;
}

.search-input:focus::placeholder {
    transform: translateX(5px);
    opacity: 0.7;
}


</style>







<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top border-bottom">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><i class="fa-solid fa-compass" ></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        
        
        <li class="nav-item">
          <a class="nav-link" href="/listings">Explore</a>
        </li>
        
      </ul>
    </div>
    <div class="search-bar-container">
      <i class="fas fa-search search-icon"></i>
      <input type="search" 
             class="form-control search-input" 
             placeholder="Discover amazing places..."
             aria-label="Search">
      <button class="btn search-button">
          <i class="fas fa-search"></i>
          <span>Search</span>
      </button>
  </div>
    <div class="collapse navbar-collapse user" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/add">Airbnb your home</a>
        <%if(!currentuser){%>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/login"><b>login</b></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/signup"><b>signup</b></a>
        </li><%}
        else{%>
        <li class="nav-item">
          <a class="nav-link" href="/logout"><b>logout</b></a>
        </li>
      <%}%>
      </ul>
    </div>
  </div>
</nav>